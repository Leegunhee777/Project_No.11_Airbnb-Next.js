import React, { useState } from 'react';
import styled from 'styled-components';

const Container = styled.div`
  padding: 50px 80px;
  margin: auto;
`;

const RoomManagement: React.FC = () => {
  return <Container>예약관리</Container>;
};
export default RoomManagement;
